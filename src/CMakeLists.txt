add_subdirectory(bch)
add_subdirectory(channel)
add_subdirectory(capture)
add_subdirectory(split)
add_subdirectory(corrupt)
add_subdirectory(utils)

if(WITH_QTGUI)
    add_subdirectory(qtgui)
endif()
